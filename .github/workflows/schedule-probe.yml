name: Temporary schedule probe
on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'
permissions: {}
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - run: date -u
